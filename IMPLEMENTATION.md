# GitHub Stats - 完全自主解决方案

本仓库实现了完全自主的 GitHub 统计卡片生成系统，无需依赖任何外部服务。

## 🎯 解决的问题

- ❌ `camo.githubusercontent.com` 图片加载失败
- ❌ `github-readme-stats.vercel.app` 服务不稳定
- ❌ 第三方服务限流和访问速度问题

## ✨ 解决方案特点

- ✅ **完全自主**: 不依赖任何第三方服务
- ✅ **GitHub API**: 直接从 GitHub 获取实时数据
- ✅ **自动更新**: 每天自动运行，保持数据最新
- ✅ **可靠稳定**: 图片存储在仓库，永久可访问
- ✅ **高度可定制**: 可自由修改样式和内容
- ✅ **健壮设计**: 完善的错误处理机制

## 📁 文件结构

```
.
├── .github/
│   └── workflows/
│       └── update-stats.yml       # GitHub Actions 自动化工作流
├── assets/                         # 生成的 SVG 图片存储目录
│   ├── stats.svg                  # GitHub 统计卡片
│   ├── top-langs.svg              # 编程语言统计
│   ├── simple-my-blog-pin.svg     # 仓库卡片
│   └── linx-pin.svg               # 仓库卡片
├── scripts/
│   └── generate_stats.py          # 统计生成脚本
├── README.md                       # 主 README（使用本地图片）
└── IMAGES_SOLUTION.md             # 详细文档
```

## 🚀 快速开始

### 1. Fork 本仓库

### 2. 启用 GitHub Actions
确保在仓库设置中启用了 GitHub Actions。

### 3. 手动触发首次更新
1. 进入 "Actions" 标签
2. 选择 "Update Stats Images"
3. 点击 "Run workflow"

### 4. 等待自动更新
之后每天 UTC 00:00 会自动更新统计数据。

## 🔧 自定义

### 修改统计卡片样式

编辑 `scripts/generate_stats.py` 文件中的生成函数：

```python
def generate_stats_card(stats):
    # 修改颜色、字体、布局等
    ...

def generate_languages_card(languages):
    # 自定义语言卡片
    ...

def generate_repo_pin_card(repo_info):
    # 自定义仓库卡片
    ...
```

### 添加更多仓库卡片

在 `scripts/generate_stats.py` 的 `main()` 函数中添加：

```python
repos_to_pin = [
    ('simple-my-blog', 'simple-my-blog-pin.svg'),
    ('Linx', 'linx-pin.svg'),
    ('your-repo', 'your-repo-pin.svg')  # 添加你的仓库
]
```

然后在 README.md 中引用：

```markdown
[![your-repo](assets/your-repo-pin.svg)](https://github.com/username/your-repo)
```

### 修改更新频率

编辑 `.github/workflows/update-stats.yml` 中的 cron 表达式：

```yaml
schedule:
  - cron: '0 0 * * *'  # 每天 00:00 UTC
  # - cron: '0 */6 * * *'  # 每 6 小时
  # - cron: '0 0 * * 0'  # 每周日
```

## 📚 技术栈

- **Python 3.11**: 脚本语言
- **requests**: HTTP 请求库
- **GitHub API v3**: 数据来源
- **GitHub Actions**: 自动化平台
- **SVG**: 矢量图形格式

## 🔍 工作原理

1. **GitHub Actions** 按计划触发工作流
2. **Python 脚本** 调用 GitHub API 获取数据
3. **生成 SVG** 卡片并保存到 `assets/` 目录
4. **自动提交** 更新的图片文件
5. **README** 引用本地图片，实现自托管

## 📖 详细文档

查看 [IMAGES_SOLUTION.md](IMAGES_SOLUTION.md) 获取更详细的说明。

## ⚠️ 注意事项

- 确保仓库有 Actions 的写入权限
- GitHub API 有速率限制，但对于日常使用足够
- 图片会占用仓库空间，但 SVG 文件很小

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可

MIT License
